#include <iostream>
using namespace std;
int main()
{
    int a,b,c,x,y;
    cin>>a>>b>>c;
    x=a;
    y=b;
    if ((c>a)&&(b>=a))
    x=c;
    else if ((c>a)&&(b<a))
    y=c;
    cout<<x+y;
    return 0;
}
